# Canonical player identity map.
#
# Each top-level key is a canonical_id (slug, lowercase, no spaces — used in URLs and joins).
# `display` is what's shown in the UI. `login` is the hub account login if known.
# `variants` are exact in-game name strings (after color-code stripping + lowercasing)
# that should resolve to this canonical_id.
#
# The canonicalizer auto-merges color-code variants (cr\efnus → cronus) and runs
# fuzzy matching with a threshold. When fuzzy matches are uncertain they go into
# .review_queue.yaml for manual decision.
#
# To override auto-grouping, list the normalized name under the correct canonical_id.
# To explicitly REJECT a fuzzy auto-merge, add the variant under `excludes` of the
# canonical it should NOT belong to.

cronus:
  display: Cronus
  login: ""
  variants:
    - cronus
  # Joke / spoof accounts that fuzzy-match cronus but ARE NOT him:
  excludes:
    - cronie
    - cronos
    - notcronus
    - "cronus's fanclub"
    - "lets go cronus"
    - "hi cronus"
    - "give up cronus"
    - "cronus made me"
    - "cronus small pp"
    - "crone is him"
    - cronorifistishim
    - "cronus is himski"
    - cronii

sane:
  display: sane
  login: ""
  # Confirmed by Cronus: same person plays under multiple names
  variants:
    - sane
    - enas
    - bposane     # added 2026-05-20 — pending DB rebuild
    - anton       # 2026-08-24 (Peter): anton is sane
    - ant0n       # 2026-08-31 fingerprint: 5 blowout wins vs cronus 2024-06..2026-03,
                  #   LG 36.1% / DDR-vs-cronus 2.15 ≈ sane career (34.9/2.04); anton-morph
    - enasio      # 2026-08-31 fingerprint: 5-game single-night farm 2024-08-19 (aerowalk),
                  #   LG 35.2% / DDR 2.35; enas-morph (enas = sane)
    # messi merged 2026-08-17 (Peter): smurf run 07-11..07-23 vs cronus (22-0).
    # Evidence: sane silent under own name 07-05..07-28 exactly spanning the run;
    # elite NA fingerprint LG .373 / DDR 1.86 ≈ sane .348/1.70; mu 2297 ≈ sane.
    - messi

grl:
  display: grl
  login: ""
  # Confirmed by Cronus: grl and grldc are the same person
  # Pending DB rebuild — added 2026-05-20
  variants:
    - grl
    - grldc

reload:
  display: reload
  login: ""
  # Confirmed by Cronus: 'notreload' = reload (same person, alt account)
  # Note: 'reload.au' is a separate straggler (7 matches) — not merged per Cronus's call.
  # If a low-match canonical isn't profiled, it doesn't matter anyway.
  variants:
    - reload
    - notreload

yeti:
  display: yeti
  login: ""
  # Confirmed by Cronus: yeti and notyeti are the same person
  # Pending DB rebuild — added 2026-05-20
  variants:
    - yeti
    - notyeti

chris:
  display: chris
  login: ""
  # Confirmed by Cronus: chris/chr1s/Eatmyass are the same person
  # Added 2026-05-21 — apply via: python canonicalize.py
  variants:
    - chris
    - chr1s
    - eatmyass
    # chris+back merged 2026-08-17 (Peter): one-night name 08-04; chris absent
    # under own name 08-03..08-05; LG .423 matches chris's elite .386 LG.
    - chris+back
    - chris_back
    - chri$       # 2026-08-31 fingerprint: 6 blowout wins vs cronus over two nights
                  #   2024-09-03..05 (all aerowalk), LG 39.2% ≈ chris career 38.6%; chris-morph

war:
  display: war
  login: ""
  # Confirmed by Cronus 2026-05-21: whodat, george, notgeorge all = war
  # 2026-08-24 (Peter): he "loves to alias" — folded in dat_nigga (74g),
  # fok (117g), allufok (102g), g_e_o_r_g_e (underscored form the normalizer
  # missed, 18g), tardface (145g), igor (214g), pikachu (572g)
  variants:
    - war
    - whodat
    - george
    - notgeorge
    - dat_nigga
    - "dat nigga"
    - datnigga
    - fok
    - allufok
    - g_e_o_r_g_e
    - tardface
    - igor
    - pikachu
    - georges     # 2026-09-04 (Peter): played as georges on The-Den 4on4s; confirmed war

blood_dog_d_p:
  # (D_P) clan tag; the closing ')' was being trimmed and the prefix-tag spelling
  # split into a stray canonical. Fixed display + merged here 2026-06-05.
  display: BLooD_DoG(D_P)
  login: BLooD_DoG
  variants:
    - "blood_dog(d_p"      # BLooD_DoG(D_P)  — tag as suffix (normalized)
    - "d_p)blood_dog"      # (D_P)BLooD_DoG  — tag as prefix (normalized)

tiba:
  # Merged 2026-06-06 (Cronus): sr.tiba + (1) variants are the same player.
  display: tiba
  variants:
    - tiba
    - sr.tiba
    - "1)tiba"
    - "1)sr.tiba"

nin:
  # Merged 2026-06-06 (Cronus): nintendo + nin7endo gimmick = nin.
  display: nin
  variants:
    - nin
    - nintendo
    - nin7endo

dusty:
  # Merged 2026-06-10: pierre is dusty (same QW login 'dusty').
  display: dusty
  login: dusty
  variants:
    - dusty
    - pierre

nico:
  # Merged 2026-06-14: Venator is Nico (per Cronus). His ZD frags were dropping
  # off team totals because "venator" wasn't linked to the "nico" canonical.
  display: Nico
  login: ""
  variants:
    - nico
    - venator
    - vyper       # 2026-09-04 (Peter): Vyper on The-Den 4on4s = Nico (9 games as vyper since 08-29)

riggz:
  # Merged 2026-07-10 (Peter): all Riggz aliases and low-count socks fold into
  # the main Riggz identity. Includes "Riggs" (typo/alt spelling) plus the
  # sockpuppet variants that were showing up as separate low-match canonicals
  # (riggzhatesyou 4, notriggz 5, riggz420691488 2, hoodratriggz 1) — total
  # ~348 matches after re-canonicalization.
  display: Riggz
  login: ""
  variants:
    - riggz
    - riggs
    - riggzhatesyou
    - notriggz
    - riggz420691488
    - hoodratriggz

sn:
  # Pinned 2026-07-15: prod already resolves "s][n" (color-coded brackets) to
  # this canonical via player_name_map — verified in hub game 226682/226688
  # debug. Pinning the variant here just prevents future drift if the name_map
  # row is ever lost. (The KOTH ch46 flag was NOT an sn issue — it was a warmup
  # game with a stand-in player "fe4r" polluting the bo3 detection window.)
  display: sn
  login: sinistro
  variants:
    - sn
    - "s][n"

powerzord:
  # Merged 2026-08-17 (Peter, from the KOTH scoreboard): "Zord" and "PowerZord"
  # are the same player. Canonical is `powerzord` because that's the id on the
  # Ultra Violence ladder roster — flipping it would break the roster link.
  # This split broke KOTH ch62 auto-resolve exactly like the canino case: Zord's
  # frags landed on a separate canonical, so UV's totals were undercounted and
  # a_n came back 1, tripping the incomplete-roster gate on maps 1-3.
  # zord = 718 games, powerzord = 368.
  display: PowerZord
  login: ""
  variants:
    - powerzord
    - zord
    - buns_world  # 2026-08-24 (Peter): bunsworld is zord
    - "buns world"
    - bunsworld

wdcanino:
  # Merged 2026-07-29 (Peter): ALL canino clan-tag variants are the same player
  # (his Discord global_name is "Caninoslz" — slz confirmed his). Plain
  # "canino" splitting into its own canonical broke KOTH ch55 auto-resolve
  # (his frags vanished from CoCaComPão's totals). Full set folded in:
  # canino (129), caninowd (5), shcanino (64), slz_canino (3) → 1,159 total.
  display: wdcanino
  login: ""
  variants:
    - wdcanino
    - wd.canino
    - canino
    - caninowd
    - shcanino
    - sh.canino
    - slz_canino
    - slz.canino

anza_fu:
  # Merged 2026-08-27 (Peter): clan-FU tag rearrangements + tagless form are
  # one player. anza_fu (5,655g) absorbs fu_anza (304g, carried his duel
  # rating), anza (246g), anzaq (8g), and the (1) dupe-suffix form.
  display: Anza FU
  login: ""
  variants:
    - anza_fu
    - "anza fu"
    - "fu/anza"
    - fu_anza
    - anza
    - anzaq
    - "1)anza fu"
  excludes:
    - noranza          # "not anza" joke account
    - "anza fanclub"
    - anza-hto         # combo/spectator name, not him alone

hto_fu:
  # Merged 2026-08-27 (Peter): same clan-tag pattern as anza.
  display: Hto FU
  login: ""
  variants:
    - hto_fu
    - "hto fu"
    - hto
    - htowski
    - hto.skogsmaskin
    - hto_skogsmaskin

blaze:
  # Created 2026-08-17 (Peter): BLZE (one game vs cronus 08-04) is blaze.
  # blaze is flagged unrated — the merge folds the game into his hidden arc.
  display: blaze
  login: ""
  variants:
    - blaze
    - blze
